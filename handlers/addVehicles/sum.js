const AddNumbers = (num1, num2) => {
  return (parseInt(num1) + parseInt(num2));
};

const MultiplyNumbers = (num1, num2) => {
  return (parseInt(num1) *parseInt(num2));
};

module.exports = {
  AddNumbers,
  MultiplyNumbers
};
  